[build]
  command = "npm install"
  publish = "."

[functions]
  node_bundler = "esbuild"
  directory = "functions"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server/:splat"
  status = 200
